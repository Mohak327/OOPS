// To check if two numbers are equal without using arithmetic operators or comparison operators.
#include <iostream>
#include<conio.h>

using namespace std;

#define N 10

// By default the first argument given to main sets its value equal to 1.
int main(int num) {
    //The if body is empty and since the conditions are all true, they are executed.
    if (num <= N && (cout<<" "<<num) && main(num + 1)) {
    }
}